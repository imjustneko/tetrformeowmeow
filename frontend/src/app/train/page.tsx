import { PlaceholderShell } from '@/components/layout/PlaceholderShell';

export default function TrainPage() {
  return (
    <PlaceholderShell
      title="Training"
      description="Lesson list and progress from the API will appear here. Your Prisma models already include lessons and progress — we will surface them in a future pass."
    />
  );
}
